# Build outputs
*.iml
.gradle/
/build/
/app/build/
/captures
.externalNativeBuild
.cxx

# IDE
/.idea/
.DS_Store

# Local config (secrets, device-specific paths)
local.properties

# Secrets / keystore (never commit)
# NOTE: gradle.properties is deliberately NOT ignored -- it carries non-secret
# build settings (android.useAndroidX etc.) that the build requires. Signing
# secrets belong in ~/.gradle/gradle.properties, outside this repo.
**/keystores/
*.jks
*.keystore

# Kotlin
.kotlin/

# Agent / dev toolkit (not part of the app)
gstack/
.agent/
AGENTS.md
CLAUDE.md
GEMINI.md
